<h1>确认商品信息</h1>
<div class="cart">
  <div class="cart-table">
    <table>
      <thead>
          <tr>
              <th class="th1">商品信息</th>
              <th class="th2"></th>
              <th class="th3">单价（元）</th>
              <th class="th4">数量</th>
              <th class="th5">金额（元）</th>
          </tr>
      </thead>
      <tbody class="goodsItemArea">
        {{#each orderPackageItemList}}
          <tr>
            <th colspan="5">
                <span class="icon icon82"></span><span class="order-confirm-from">{{shippingWarehouse}}</span><span class="order-confirm-time">{{shippingTime}}</span>
            </th>
          </tr>
          {{#each orderGoodsItemList}}
          <tr>
            <td class="td1">
                <a href="javascript:void(0)"><img src="{{sf.img productImage.thumbImgUrl}}"></a>
                <div class="order-confirm-info">
                    <h2><a href="javascript:void(0)">{{goodsName}}</a></h2>
                    <div class="cart-standard">{{{spec}}}</div>
                </div>
            </td>
            <td class="td2"></td>
            <td class="td3">{{sf.price price}}</td>
            <td class="td4">{{quantity}}</td>
            <td class="td5">
                <strong class="text-error">{{sf.price totalPrice}}</strong>
            </td>
          </tr>
          {{/each}}
        {{/each}}

      </tbody>
    </table>
  </div>
  {{! end cart-table }}
  <div class="cart-coupon">
    <div class="cart-coupon-r1"><a href="javascript:void(0)" id="coupon-use">-</a>使用优惠券</div>
    <div id="coupon-use-detail">
      {{^orderCoupon.isHaveAvaliable}}
        <p>此订单暂无可用的优惠券。您可以 <a class="coupon2-link" target="_blank" href="{{links.coupon}}">[查看所有优惠券]</a>了解使用限制。    <a class="coupon2-link" target="_blank" href="http://www.sfht.com/helpcenter-coupon.html">[优惠券使用规则]</a></p>
      {{/orderCoupon.isHaveAvaliable}}
      {{#orderCoupon.isHaveAvaliable}}
        <div class="coupon" data-price="{{orderCoupon.bestCoupon.price}}" data-code="{{orderCoupon.bestCoupon.couponCode}}" id="useCoupon">
          <div class="coupon-r1"><b>¥</b><strong>{{sf.price orderCoupon.bestCoupon.price}}</strong>{{orderCoupon.bestCoupon.couponName}}</div>
          <div class="coupon-r2">有效期至：{{orderCoupon.bestCoupon.endDate}}</div>
          <span class="icon icon85"></span>
        </div>
      {{/orderCoupon.isHaveAvaliable}}
      {{#orderCoupon.isShowMore}}
      <div class="cart-coupon-r2">
        <a href="javascript:void(0)" class="text-link" id="coupon-more"><span id="coupon-more-text">展开更多</span><span class="icon icon16-1"></span></a>
      </div>
      <div class="coupons" id="coupons">
        <ul class="coupons-tab">
          <li class="active"><a href="javascript:void(0)">可用优惠券</a></li>
          <li><a href="javascript:void(0)">不可用优惠券</a></li>
        </ul>
        {{! 可用优惠券 }}
        <ul id="avaliableCoupons" class="coupons-list active">
          {{#each orderCoupon.avaliableCoupons}}
            <li {{data 'coupon'}}>
              <div class="coupon">
                  <div class="coupon-r1"><b>¥</b><strong>{{sf.price price}}</strong>{{couponName}}</div>
                  <div class="coupon-r2">有效期至：{{endDate}}</div>
              </div>
            </li>
          {{/each}}
        </ul>
        {{! 不可用优惠券 }}
        <ul id="disableCoupons" class="coupons-list">
          {{#each orderCoupon.disableCoupons}}
            <li {{data 'coupon'}}>
              <div class="coupon coupon-disable">
                  <div class="coupon-r1"><b>¥</b><strong>{{sf.price price}}</strong>{{couponName}}</div>
                  <div class="coupon-r2">有效期至：{{endDate}}</div>
              </div>
              <div class="text-error">{{desc}}!</div>
            </li>
          {{/each}}
        </ul>
      </div>
      {{/orderCoupon.isShowMore}}
      {{! end 优惠券列表 }}
      <div class="cart-coupon-r3">or<a href="javascript:void(0)" class="text-link">兑换优惠券？</a></div>
      <div class="cart-coupon-r4">请输入您手中的兑换码：
        <input class="input" type="text" can-value="orderCoupon.couponExCode" value="{{orderCoupon.couponExCode}}" id="conpon" />
        <a id="useCouponCodeBtn" class="btn btn-normal" href="#">确定</a>
      </div>
    </div>
  </div>
  {{! end cart-coupon }}
  <div class="cart-footer clearfix">
    <div class="cart-footer-c2 fr">
        <ul>
            <li class="li1"><span class="s1">商品总计 :</span>¥{{sf.price orderFeeItem.goodsTotalFee}}</li>
            <li class="li1">
                <span class="s1">活动优惠 :</span>- ¥ {{sf.price orderFeeItem.discount}}
            </li>
            <li class="li1"><span class="text-important">-优惠券：￥{{sf.price couponPrice}}</span></li>
            <li class="li1">
                <div class="tooltip-cart"><span class="icon icon83 fl"></span><span class="text-error">亲：我们已经任性的给您包邮啦！</span><span class="icon icon16-5"></span><span class="icon icon16-5 icon16-5-1"></span></div>
                <span class="s1">运费 :</span>免运费
            </li>
            <li><span class="s1">总价（含税费） :</span><strong class="text-error">¥{{sf.price orderFeeItem.shouldPay}}</strong></li>
        </ul>

        <button id="submitOrder" class="btn btn-danger {{^flag}} btn-disable{{/flag}}">提交订单</button>
        <div class="text-error">{{^flag}}{{errorDes}}{{/flag}}</div>

    </div>
    <span class="icon icon75"></span>
    <span class="icon icon76"></span>
  </div>
  {{! end  cart-footer}}
</div>
